<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Mach-O 加载时的动态链接</title>
    <link href="/posts/a5ddcb7c.html"/>
    <url>/posts/a5ddcb7c.html</url>
    
    <content type="html"><![CDATA[<blockquote><p><a href="https://alphabyte.tk/posts/a58100d4.html">Mach-O 文件结构详解</a>中分析了 Mach-O 的构成，介绍了部分 <code>Segment</code> 和 <code>Section</code> 的结构和其作用，相当于是静态分析。这篇文章将分析 Mach-O 加载时动态链接过程，加深自己的理解。</p></blockquote><h2 id="启动过程"><a href="#启动过程" class="headerlink" title="启动过程"></a>启动过程</h2><p>iOS App 的启动过程大概分为这几个步骤：</p><ol><li>内核初始化部分，负责将 App 的 Mach-O Header 映射到内存中进行处理然后调用 <code>dyld</code></li><li><code>dyld</code> 负责将 App 处理为一个可以运行的状态，通过添加 <code>DYLD_PRINT_STATISTICS</code> 环境变量可以查看各个阶段，包括：<ul><li>加载 Mach-O 的依赖库。所依赖的库信息在 Mach-O 的 <code>Load Commands</code> 中可以看到</li><li>Fix-ups，地址修正。当所依赖的动态链接库加载完成后，它们彼此间独立，该阶段负责将它们绑定起来。其中 Fix-ups 包括两部分：<ol><li><code>Rebasing</code>：将 App 可行文件中指向内部的指针进行指向修正</li><li><code>Binding</code>：修复 App 可执行文件中指向外部的指针</li></ol></li><li>ObjC 环境配置。该阶段进行<code>Class</code>、<code>Category</code>的注册和<code>SEL</code>的分配。</li><li>initializer 。<code>+load</code> 在该阶段执行</li></ul></li><li>进入 <code>main()</code> 和 <code>UIApplicationDelegate</code> 回调状态执行阶段，展示自定义 UI</li></ol><h2 id="符号绑定（Binding）"><a href="#符号绑定（Binding）" class="headerlink" title="符号绑定（Binding）"></a>符号绑定（Binding）</h2><p>Mach-O 加载时，符号分成了懒加载符号和非懒加载符号，非懒加载符号在 <code>dyld</code> 加载 Mach-O 时就会绑定真实地址；对于来自于系统动态链接库或者独立于 Main Execute Binary（Mach-O） 之外的懒加载符号，只在在它们第一次被调用时才会绑定真实地址，后续调用时将直接使用真实地址。</p><p>Demo 二进制 <a href="https://github.com/Navimark/MachOExploration/blob/master/HelloWorld/HelloWorld" target="_blank" rel="noopener">HelloWorld</a> 中调用了来自于系统动态库的 <code>NSLog</code>，下面将通过该二进制及其源码来分析符号的绑定过程。</p><p>下面会先使用 MachOView 静态查看相关的 Section 和 Segment 静态分析，然后单步动态调试验证。</p><h4 id="1-TEXT-stubs"><a href="#1-TEXT-stubs" class="headerlink" title="1. __TEXT,__stubs"></a>1. <code>__TEXT,__stubs</code></h4><p><code>__TEXT</code> 段存放的是代码指令，其中 <code>__TEXT,__stubs</code> 存放的是当前 <code>Image（镜像）</code> 外部符号的桩点。下图是 <code>HelloWorld</code> 的 <code>__TEXT,__stubs</code> 部分：<br><img src="a5ddcb7c/1.png" srcset="/img/loading.gif" alt="__stubs"><br>其中 Data 字段是机器码，可以按照 AArch64 指令集的编码规则<sup id="fnref:1" class="footnote-ref"><a href="#fn:1" rel="footnote"><span class="hint--top hint--rounded" aria-label="ARM64 汇编——寄存器和指令">[1]</span></a></sup>翻译成汇编代码，使用 Hopper Disassembler 打开二进制（注意不要勾选 Mach-O AArch64 Options 的 Resolve Lazy Bindings 选项），定位到 <code>NSLog</code>：<br></p><div class="group-image-container"><div class="group-image-row"></div><div class="group-image-row"><div class="group-image-wrap"><img src="a5ddcb7c/2.png" srcset="/img/loading.gif" alt="NSLog1"></div><div class="group-image-wrap"><img src="a5ddcb7c/3.png" srcset="/img/loading.gif" alt="NSLog2"></div><div class="group-image-wrap"><img src="a5ddcb7c/4.png" srcset="/img/loading.gif" alt="NSLog3"></div></div></div><br>Hopper 右边 Instruction Encoding 区域的 16 进制串对应当前选中的汇编命令，三条命令组合起来的 16 进制串 <code>1F2003D510D7005800021FD6</code> 刚好就是 MachOView 中 <code>NSLog</code> 的 Data 值。这三行指令让跳转到 <code>0x100008008</code> 去执行，<code>0x100008008</code> 减去 VM(<code>(LC_SEGMENT_64),__PAGEZERO.VMSize</code>) 的初始偏移值：<code>0x100000000</code>(4294967296) ，得到 <code>0x000008008</code>，它对应 Mach-O 中 <code>__DATA.__la_symbol_ptr</code> 的起始位置。<br><img src="a5ddcb7c/5.png" srcset="/img/loading.gif" alt="__stubs"><br>由于 <code>__TEXT</code> 是只读的，为了能够在 Mach-O 经过了 ASLR 和 PIE 后仍然能够正确地定位外部符号的地址，完成动态链接，<code>__TEXT,__stubs</code> 充当了跳板作用，当有人要调用 stub 符号时，它便将调用流程转向了一个可写区域 <code>__DATA.__la_symbol_ptr</code>，而该区域将存放真正的符号地址<p></p><h4 id="2-DATA-la-symbol-ptr"><a href="#2-DATA-la-symbol-ptr" class="headerlink" title="2. __DATA.__la_symbol_ptr"></a>2. <code>__DATA.__la_symbol_ptr</code></h4><p><code>__la_symbol_ptr</code> 存放 lazy-binding 的函数指针信息，在 Hopper 中双击 <code>NSLog</code> 的地址 <code>0x100008008</code>，将会跳转到：<br><img src="a5ddcb7c/6.png" srcset="/img/loading.gif" alt="__stubs"><br>是一个交叉引用地址，指向 <code>0x00000001000065cc</code>，而该地址落在 <code>__TEXT.__stub_helper</code> 中，且该处的 Data 为 <code>50000018</code>：<br><img src="a5ddcb7c/7.png" srcset="/img/loading.gif" alt="__stubs"><br>对应的汇编代码会将流程转向 <code>__TEXT.__stub_helper</code> 的头部<br><img src="a5ddcb7c/8.png" srcset="/img/loading.gif" alt="__stubs"></p><h4 id="3-TEXT-stub-helper"><a href="#3-TEXT-stub-helper" class="headerlink" title="3. __TEXT.__stub_helper"></a>3. <code>__TEXT.__stub_helper</code></h4><p><code>__TEXT.__stub_helper</code> 区域是一段汇编代码，这段代码不仅会找到要调用符号真正的地址并跳转到地址执行，而且会将该地址写回对应的 <code>__DATA.__la_symbol_ptr</code>，实现“缓存”。</p><h4 id="NSLog-的绑定过程"><a href="#NSLog-的绑定过程" class="headerlink" title="NSLog 的绑定过程"></a><code>NSLog</code> 的绑定过程</h4><ul><li><p>新建一个 <a href="https://github.com/Navimark/MachOExploration/blob/master/HelloWorld/ViewController.m" target="_blank" rel="noopener">demo</a> ，主要代码如下：</p><pre><code class="hljs objc">- (<span class="hljs-keyword">void</span>)viewDidLoad &#123;  [<span class="hljs-keyword">super</span> viewDidLoad];  <span class="hljs-comment">// Do any additional setup after loading the view.</span>  <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"1"</span>);  <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"2"</span>);&#125;</code></pre><p>在两个 <code>NSLog</code> 处分别下断点，设置 Debug Workflow 为 Always Show Disassembly，然后选择 arm64 真机运行，如图：<br><img src="a5ddcb7c/9.png" srcset="/img/loading.gif" alt="__stubs"><br>Xcode 提示当前位置为 <code>symbol stub for NSLog</code></p></li><li><p>得到 ALSR 偏移：<code>0x102ea0000</code></p><pre><code class="hljs sh">(lldb) image list HelloWorld  [  0] 1F21C492-D5E8-3B0B-B239-E0E691E7D64F 0x0000000102ea0000 /Users/chenzheng/Library/Developer/Xcode/DerivedData/MachOExploration-gyejyvbqloguepctiipvnexsjqsv/Build/Products/Debug-iphoneos/HelloWorld.app/HelloWorld</code></pre></li><li><p>执行 lldb step into 命令<code>stepi</code>，如下：</p><pre><code class="hljs armasm"><span class="hljs-symbol">HelloWorld</span>`NSLog:-&gt;  <span class="hljs-number">0x102ea6568</span> &lt;+<span class="hljs-number">0</span>&gt;: <span class="hljs-keyword">nop </span>       <span class="hljs-number">0x102ea656c</span> &lt;+<span class="hljs-number">4</span>&gt;: <span class="hljs-keyword">ldr </span>   x16, <span class="hljs-number">#0x1a9c</span>   <span class="hljs-comment">; (void *)0x0000000102ea6610</span>    <span class="hljs-number">0x102ea6570</span> &lt;+<span class="hljs-number">8</span>&gt;: <span class="hljs-keyword">br </span>    x16</code></pre><p>按住 Control 键点 Step Over 达到 <code>ldr</code> 所在行，该行取 (<code>pc + 0x1a9c</code>) 处的地址（经过计算为 <code>0x0000000102ea8008</code>），存入 <code>x16</code>，该地址指向 <code>__DATA.__la_symbol_ptr</code> 中偏移为 <code>0x0</code> 的位置，由 MachOView 可知该处即是 <code>_NSLog</code> 的位置，对应，如下图：<br><img src="a5ddcb7c/10.png" srcset="/img/loading.gif" alt="__stubs"></p><h4 id="真实地址的所在"><a href="#真实地址的所在" class="headerlink" title="真实地址的所在"></a>真实地址的所在</h4><ul><li>在 <code>0x0000000102ea6610</code> 处下断点:<code>br set -a 0x0000000102ea6610</code>，然后继续执行，如下：<pre><code class="hljs armasm">-&gt;  <span class="hljs-number">0x102ea6610</span>: <span class="hljs-keyword">ldr </span>   w16, <span class="hljs-number">0x102ea6618</span>  <span class="hljs-number">0x102ea6614</span>: <span class="hljs-keyword">b </span>     <span class="hljs-number">0x102ea65f8</span>  <span class="hljs-number">0x102ea6618</span>: udf    <span class="hljs-number">#0x0</span>  <span class="hljs-number">0x102ea661c</span>: <span class="hljs-keyword">ldr </span>   w16, <span class="hljs-number">0x102ea6624</span>  <span class="hljs-number">0x102ea6620</span>: <span class="hljs-keyword">b </span>     <span class="hljs-number">0x102ea65f8</span>  <span class="hljs-number">0x102ea6624</span>: udf    <span class="hljs-number">#0xd</span>  <span class="hljs-number">0x102ea6628</span>: <span class="hljs-keyword">ldr </span>   w16, <span class="hljs-number">0x102ea6630</span>  <span class="hljs-number">0x102ea662c</span>: <span class="hljs-keyword">b </span>     <span class="hljs-number">0x102ea65f8</span></code></pre>结合第一行和第三行，<code>0x102ea6610</code> 处的 <code>ldr</code> 指令实际是取文件 <code>0x6618</code> 处指示的立即数 <code>0x00000000</code> 存入 <code>x16</code> 中，这个 <code>0x00000000</code> 是一个偏移值，描述的是距离Dynamic Loader Info 中 Lazy Binding Info 起始地址的偏移，从下图可以看出，偏移后位置为 <code>0x0000c2a8 + 0x00000000</code><br><img src="a5ddcb7c/11.png" srcset="/img/loading.gif" alt="__stubs"><br>其中 <code>dylib(1)</code> 表示该符号处于当前文件第三个 LC_LOAD_DYLIB 中，即 <code>libSystem.B.dylib</code> 中；segment(2) 和 offset(8) 表示将找到的真实地址写入当前文件架构的第 2 个 segment 偏移为 8 的地方。从 MachOView 中可以看到，第二个 segment 即为 __DATA，偏移 8 的位置刚好是 <code>_NSLog</code> 的符号<div class="group-image-container"><div class="group-image-row"></div><div class="group-image-row"><div class="group-image-wrap"><img src="a5ddcb7c/12.png" srcset="/img/loading.gif" alt="NSLog1"></div><div class="group-image-wrap"><img src="a5ddcb7c/13.png" srcset="/img/loading.gif" alt="NSLog2"></div><div class="group-image-wrap"><img src="a5ddcb7c/14.png" srcset="/img/loading.gif" alt="NSLog3"></div></div></div></li></ul></li><li><p>绑定与执行<br>设置断点 <code>br set -a 0x102ea65f8</code> 继续执行，如下：</p><pre><code class="hljs armasm"><span class="hljs-number">0x102ea65f8</span>: <span class="hljs-keyword">adr </span>   x17, <span class="hljs-number">#0x2fb8</span>  <span class="hljs-comment">; _dyld_private</span><span class="hljs-number">0x102ea65fc</span>: <span class="hljs-keyword">nop </span>   <span class="hljs-number">0x102ea6600</span>: stp    x16, x17, [<span class="hljs-built_in">sp</span>, #-<span class="hljs-number">0x10</span>]!<span class="hljs-number">0x102ea6604</span>: <span class="hljs-keyword">nop </span>   <span class="hljs-number">0x102ea6608</span>: <span class="hljs-keyword">ldr </span>   x16, <span class="hljs-number">#0x19f8</span>  <span class="hljs-comment">; (void *)0x00000001b51bc80c: dyld_stub_binder</span><span class="hljs-number">0x102ea660c</span>: <span class="hljs-keyword">br </span>    x16<span class="hljs-number">0x102ea6610</span>: <span class="hljs-keyword">ldr </span>   w16, <span class="hljs-number">0x102ea6618</span><span class="hljs-number">0x102ea6614</span>: <span class="hljs-keyword">b </span>     <span class="hljs-number">0x102ea65f8</span></code></pre><p><code>dyld_stub_binder</code> 和真实地址查找有关，在此处下断点：<code>br set -a 0x00000001b51bc80c</code> 后继续执行，进入到 <code>dyld</code> 内部，单步执行到 <code>_dyld_fast_stub_entry</code> 所在行，如图：<br><img src="a5ddcb7c/15.png" srcset="/img/loading.gif" alt="NSLog1"></p><p>在这一行的前两行:</p><pre><code class="hljs armasm"><span class="hljs-number">0x1b51bc83c</span> &lt;+<span class="hljs-number">48</span>&gt;:  <span class="hljs-keyword">ldr </span>x0, [x29, <span class="hljs-number">#0x18</span>] <span class="hljs-number">0x1b51bc840</span> &lt;+<span class="hljs-number">52</span>&gt;:  <span class="hljs-keyword">ldr </span>x1, [x29, <span class="hljs-number">#0x10</span>]</code></pre><p>将 <code>x29</code> 分别偏移 <code>0x18</code> 和 <code>0x10</code> 处的地址对应的值取出来放到 <code>x0</code>、<code>x1</code>，作为参数传给了 <code>_dyld_fast_stub_entry</code>，<code>_dyld_fast_stub_entry</code> 中将完成符号地址的查找。<code>x1</code> 中就是上述提到的偏移量, 而 <code>x0</code> 是一个<code>_dyld_private</code>指针:</p><pre><code class="hljs sh">(lldb) register <span class="hljs-built_in">read</span> x1      x1 = 0x0000000000000000(lldb) register <span class="hljs-built_in">read</span> x0      x0 = 0x0000000102ea95b0  _dyld_private</code></pre></li><li><p><code>_dyld_fast_stub_entry</code><br>这是 <code>dyld</code> 中的一个函数，我们下载一套和系统中正在使用的 <code>dyld</code> 最接近的源码，尝试分析其功能。<br>在 lldb 中执行 <code>image list dyld</code>，得到 <code>dyld</code> 的路径<code>/Users/chenzheng/Library/Developer/Xcode/iOS DeviceSupport/12.4 (16G77)/Symbols/usr/lib/dyld</code>，将其拖入到 MachOView 中，定位到 <code>LC_SOURCE_VERSION</code>，看到 Version 为 650.3.4，可惜 <a href="https://opensource.apple.com/tarballs/dyld/" target="_blank" rel="noopener">dyld 源码页面</a>并没有找到匹配的版本，所以下载了 655 版本。打开 dyld 工程后定位到 <code>_dyld_fast_stub_entry</code>:</p><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-meta-keyword">if</span> __i386__ || __x86_64__ || __arm__ || __arm64__</span>__attribute__((visibility(<span class="hljs-string">"hidden"</span>))) <span class="hljs-keyword">void</span>* _dyld_fast_stub_entry(<span class="hljs-keyword">void</span>* loadercache, <span class="hljs-keyword">long</span> lazyinfo)&#123;  DYLD_NO_LOCK_THIS_BLOCK;    <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span>* (*p)(<span class="hljs-keyword">void</span>*, <span class="hljs-keyword">long</span>) = <span class="hljs-literal">NULL</span>;    <span class="hljs-keyword">if</span>(p == <span class="hljs-literal">NULL</span>)      <span class="hljs-comment">// 如果缓存位置为空，从全局的 `函数名-函数指针` 数组中找到对应的</span>      <span class="hljs-comment">// 函数指针 ((void*)dyld::fastBindLazySymbol)，然后将形参透传过去并执行</span>      <span class="hljs-comment">// 然后返回执行的结果，此结果应该就是符号对应的真实地址</span>      _dyld_func_lookup(<span class="hljs-string">"__dyld_fast_stub_entry"</span>, (<span class="hljs-keyword">void</span>**)&amp;p);  <span class="hljs-keyword">return</span> p(loadercache, lazyinfo);&#125;<span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span></span></code></pre><p>好奇一下 <code>dyld::fastBindLazySymbol</code> 的实现：</p><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">uintptr_t</span> <span class="hljs-title">fastBindLazySymbol</span><span class="hljs-params">(ImageLoader** imageLoaderCache, \</span></span><span class="hljs-function"><span class="hljs-params"> <span class="hljs-keyword">uintptr_t</span> lazyBindingInfoOffset)</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-keyword">uintptr_t</span> result = <span class="hljs-number">0</span>;  <span class="hljs-comment">// get image </span>  <span class="hljs-keyword">if</span> ( *imageLoaderCache == <span class="hljs-literal">NULL</span> ) &#123;    <span class="hljs-comment">// save in cache</span>    *imageLoaderCache = dyld::findMappedRange((<span class="hljs-keyword">uintptr_t</span>)imageLoaderCache);    <span class="hljs-keyword">if</span> ( *imageLoaderCache == <span class="hljs-literal">NULL</span> ) &#123;<span class="hljs-meta">#<span class="hljs-meta-keyword">if</span> SUPPORT_ACCELERATE_TABLES</span>    <span class="hljs-keyword">if</span> ( sAllCacheImagesProxy != <span class="hljs-literal">NULL</span> ) &#123;      <span class="hljs-keyword">const</span> mach_header* mh;      <span class="hljs-keyword">const</span> <span class="hljs-keyword">char</span>* path;      <span class="hljs-keyword">unsigned</span> index;      <span class="hljs-keyword">if</span> ( sAllCacheImagesProxy-&gt;addressInCache(imageLoaderCache, &amp;mh, \        &amp;path, &amp;index) ) &#123;        result = sAllCacheImagesProxy-&gt;bindLazy(lazyBindingInfoOffset, \         gLinkContext, mh, index);        <span class="hljs-keyword">if</span> ( result == <span class="hljs-number">0</span> ) &#123;          halt(<span class="hljs-string">"dyld: lazy symbol binding failed for image in dyld shared\n"</span>);        &#125;        <span class="hljs-keyword">return</span> result;      &#125;    &#125;<span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span></span>      <span class="hljs-keyword">const</span> <span class="hljs-keyword">char</span>* message = <span class="hljs-string">"fast lazy binding from unknown image"</span>;      dyld::<span class="hljs-built_in">log</span>(<span class="hljs-string">"dyld: %s\n"</span>, message);      halt(message);    &#125;  &#125;    <span class="hljs-comment">// bind lazy pointer and return it</span>  <span class="hljs-keyword">try</span> &#123;    result = (*imageLoaderCache)-&gt;doBindFastLazySymbol( \             (<span class="hljs-keyword">uint32_t</span>)lazyBindingInfoOffset, gLinkContext, \            (dyld::gLibSystemHelpers != <span class="hljs-literal">NULL</span>) ? \            dyld::gLibSystemHelpers-&gt;acquireGlobalDyldLock : <span class="hljs-literal">NULL</span>,(dyld::gLibSystemHelpers != <span class="hljs-literal">NULL</span>) ? \              dyld::gLibSystemHelpers-&gt;releaseGlobalDyldLock : <span class="hljs-literal">NULL</span>);  &#125;  <span class="hljs-keyword">catch</span> (<span class="hljs-keyword">const</span> <span class="hljs-keyword">char</span>* message) &#123;    dyld::<span class="hljs-built_in">log</span>(<span class="hljs-string">"dyld: lazy symbol binding failed: %s\n"</span>, message);    halt(message);  &#125;  <span class="hljs-comment">// return target address to glue which jumps to it with real parameters restored</span>  <span class="hljs-keyword">return</span> result;&#125;</code></pre><p>大致是根据传入的 <code>lazyBindingInfoOffset</code> 和 <code>gLinkContext</code> 找到真实的地址，然后返回。</p></li><li><p>再单步一次执行完 <code>_dyld_fast_stub_entry</code>，得到其返回值（存在在x0中），打印 x0 ，得到的 <code>0x00000001b61d7620</code> 正是 <code>NSLog</code> 在 Foundation 框架中的地址：<br><img src="a5ddcb7c/16.png" srcset="/img/loading.gif" alt="NSLog1"></p></li><li><p>第二次执行<br>单步执行，应该会停在第二个 <code>NSLog</code> 的地方：<br><img src="a5ddcb7c/17.png" srcset="/img/loading.gif" alt="NSLog1"><br>设置断点 <code>br set -a 0x102ea6568</code>，然后执行，在断点位置已经有提示，存入 <code>x16</code> 的地址已经是上述 <code>NSLog</code> 的地址 <code>0x00000001b61d7620</code>，经过验证，<code>__DATA.__la_symbol_ptr</code> 偏移 <code>0x00</code> 处的值已经被赋值为了 <code>0x00000001b61d7620</code><br><img src="a5ddcb7c/18.png" srcset="/img/loading.gif" alt="NSLog1"></p></li></ul><h4 id="dyld-stub-binder？"><a href="#dyld-stub-binder？" class="headerlink" title="dyld_stub_binder？"></a><code>dyld_stub_binder</code>？</h4><p><code>__TEXT.__stub_helper</code> 中本是用来帮助处理符号的延迟绑定的一段代码，里面引用了同样不在当前 Mach-O 中的符号 <code>dyld_stub_binder</code>，好像有问题🤔<br>其实 <code>dyld_stub_binder</code> 存在于 <code>__DATA.__got</code> 区域，是非延迟绑定的符号，它在 Mach-O 加载完成后立即可用，可以用来辅助定位延迟绑定符号的地址。</p><h3 id="流程总结"><a href="#流程总结" class="headerlink" title="流程总结"></a>流程总结</h3><p>上述 <code>viewDidLoad</code> 对应的汇编代码:<br><img src="a5ddcb7c/19.png" srcset="/img/loading.gif" alt="NSLog1"><br>第一次 <code>bl imp_stubs_NSLog</code> 时将跳转到 <code>NSLog</code> 的桩点，这一段汇编代码将从 <code>__DATA.__la_symbol_ptr</code> 中读取地址并跳转执行，而 <code>__DATA.__la_symbol_ptr</code> 中此时的地址指向 <code>__TEXT.__stub_helper</code> 并最终被引导到 <code>__TEXT.__stub_helper</code> 的头部执行；在 <code>__TEXT.__stub_helper</code> 中调用非延迟绑定的 <code>dyld_stub_binder</code> ，在其中通过 <code>_dyld_fast_stub_entry</code> 拿到真实的地址，调用 <code>_dyld_fast_stub_entry</code> 时传入了从 Dynamic Loader Info 中取到的当前待绑定符号的 Lazy Binding Info ，以便在找到真实地址后能够写回 <code>__DATA.__la_symbol_ptr</code> 中，下次 <code>bl imp_stubs_NSLog</code> 时将直接调用</p><p>延迟绑定技术很好地处理好了代码复用和使用效率问题，整个过程只有在自己亲自动手跟进一番才会有深刻的理解</p><section class="footnotes"><div class="footnote-list"><ol><li><span id="fn:1" class="footnote-text"><span><a href="https://www.jianshu.com/p/2f4a5f74ac7a" target="_blank" rel="noopener">ARM64 汇编——寄存器和指令</a><a href="#fnref:1" rev="footnote" class="footnote-backref"> ↩</a></span></span></li></ol></div></section>]]></content>
    
    
    <categories>
      
      <category>Mach-O</category>
      
    </categories>
    
    
    <tags>
      
      <tag>底层</tag>
      
      <tag>Mach-O</tag>
      
      <tag>加载方式</tag>
      
      <tag>lldb</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>给 GitHub Pages 博客绑定免费的个人域名</title>
    <link href="/posts/f5d9b671.html"/>
    <url>/posts/f5d9b671.html</url>
    
    <content type="html"><![CDATA[<div class="note note-success"><p>🎉🎉咱也有顶级域名了！</p></div><p>本文将记录 GitHub Pages 绑定域名的过程</p><h1 id="域名申请"><a href="#域名申请" class="headerlink" title="域名申请"></a>域名申请</h1><p><a href="https://www.freenom.com/" target="_blank" rel="noopener">Freenom</a> 是世界上第一个也是唯一的免费域名提供商，但是这家好像是不允许来自大陆的注册和续期了，而且新注册的也需要科学上网才行，所以下面的操作需要首先“云游”到外国，并且在填写注册所需的各种资料时要表现得像一个真人，以免被 Freenom 误判为机器人而申请失败。</p><ol><li>首先开启全局代理。一开始是打算为 <code>freenom.com</code> 制定规则，避免xxxx，后来担心里面的 js、啥啥啥的有问题，就开全局了</li><li>打开 Chrome 浏览器，新创建一个无痕窗口访问 <code>https://www.freenom.com</code>，输入意向的<strong>完整</strong>域名后点击 Check Availability，如果可用将如图展示：<br><img src="f5d9b671/1.png" srcset="/img/loading.gif" alt="图片"></li><li>点击 Checkout 后可以选择生效期，在 Period 中选择 <code>12 Months @ FREE</code> 然后 Continue<br><img src="f5d9b671/2.png" srcset="/img/loading.gif" alt="图片"></li><li>接下来填写邮箱地址（这个地址将成为 <code>Freenom</code> 账号登录名），既然已经云游到国外了，这里最好是填国外的邮箱服务商的邮箱地址吧，如 gmail.com 。填好后点击 Verify My Email Adress，然后到邮箱中去点击验证链接<br><img src="f5d9b671/3.png" srcset="/img/loading.gif" alt="图片"></li><li>点击验证链接后，需要填入地址信息，这里的地址需要“云游”服务器的 IP 地址所在的地址。<br><img src="f5d9b671/4.png" srcset="/img/loading.gif" alt="图片"><ol><li>访问<a class="btn" href="https://ip.sb" title="IP 查询" target="_blank">ip.sb</a>查询到“云游” IP 所在地，然后去 Google Maps 搜索到所在地的地址信息。如果 IP 在美国，建议从<a href="http://www.shenfendaquan.com/Index/index/us_top_1000_company" target="_blank" rel="noopener">这里</a>获取需要的信息</li><li>逐一填写上述得到的地址信息，填写密码</li><li>提交。不出意外的话，点击 Freenom 首页 -&gt; Services -&gt; My Domains 就能看到注册的域名了<br><img src="f5d9b671/5.png" srcset="/img/loading.gif" alt="图片"><br><img src="f5d9b671/6.png" srcset="/img/loading.gif" alt="图片"><div class="note note-success"><ul><li>只能在域名到期前 14 天续费（<a href="http://www.freenom.com/zh/freeandpaiddomains.html" target="_blank" rel="noopener">免费更新的</a>），建议设置提醒事件，避免来年忘记续费时间点</li><li>后期可以给 <code>freenom.com</code> 和 <code>*.freenom.com</code> 新添加 PAC 规则，让它们被转发:</li></ul></div></li></ol></li></ol><h1 id="DNS-解析"><a href="#DNS-解析" class="headerlink" title="DNS 解析"></a>DNS 解析</h1><p>进入域名列表后，依次点击选定域名的 Manage Domins -&gt; Manage Freenom DNS，然后填入如下规则<br><img src="f5d9b671/9.png" srcset="/img/loading.gif" alt="图片"><br>其中 IP 地址的由来：<a href="https://help.github.com/en/github/working-with-github-pages/managing-a-custom-domain-for-your-github-pages-site" target="_blank" rel="noopener">Managing a custom domain for your GitHub Pages site</a></p><h1 id="GitHub-Pages-设置"><a href="#GitHub-Pages-设置" class="headerlink" title="GitHub Pages 设置"></a>GitHub Pages 设置</h1><ol><li>进入 GitHub 上托管 Pages 仓库的 Setting 页面，找到 GitHub Pages 设置区域，在 Custom domain 中填入域名，注意不需要 http(s) 前缀，然后点保存。保存完毕后仓库的根目录下会新增加一个 <code>CNAME</code> 文件，内容为 <code>alphabyte.tk</code>，这个 <code>CNAME</code> 指导 github 是否要定向到 <code>alphabyte.tk</code></li><li>随着 DNS 服务器的递归索引的建立，等上几分钟或者几个小时，通过 <code>https://alphabyte.tk/</code> 或 <code>https://www.alphabyte.tk/</code> 就能访问到博客啦。可以在命令行执行 <code>dig alphabyte.tk</code> 命令来检测 DNS 解析是否生效。</li></ol><h1 id="Hexo-设置"><a href="#Hexo-设置" class="headerlink" title="Hexo 设置"></a>Hexo 设置</h1><p>由于每次发表文章部署 Pages 时，Pages 主页的内容都是被全量清除并覆盖的，<code>CNAME</code> 文件也将被清除，所以肯定需要让 hexo 能够管理这个 <code>CNAME</code> 文件的，所以还需要对 hexo 进行一些处理：在 hexo 主题的 source 目录下创建一个 <code>CNAME</code> 文本文件，内容为 <code>alphabyte.tk</code>，然后在 <code>_config.yml</code> 中设置<code>CNAME</code> 跳过渲染 <code>skip_render: CNAME</code>，这样每次部署博客时，<code>CNAME</code> 将会被拷贝到网站根目录。</p><h1 id="接下来"><a href="#接下来" class="headerlink" title="接下来"></a>接下来</h1><ol><li>Freenom 的 DNS 解析服务在大陆访问的延迟比较高，可以考虑接入其他更专业的第三方 DNS 服务商</li><li>由于有了顶级私有域名，现在可以定制专属 404 页面了</li><li>提交给百度收录</li></ol>]]></content>
    
    
    <categories>
      
      <category>博客装修</category>
      
    </categories>
    
    
    <tags>
      
      <tag>装修</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Fastlane Match 如何重置证书/描述文件</title>
    <link href="/posts/feb5cc21.html"/>
    <url>/posts/feb5cc21.html</url>
    
    <content type="html"><![CDATA[<p><a href="https://docs.fastlane.tools/actions/match/" target="_blank" rel="noopener">fastlane match</a> 有多香咱就不赘述了，尤其是团队开发和做 CI 时，在开发证书和测试设备管理这两方面使用 fastlane match 能节省不少时间和精力。不过有时候我们会碰到需要重新生成描述文件或重置证书的情况，比如接入 Apple Sign In 功能时，需要在证书中增加对应的能力，必须要更新描述文件时；用于生成原开发证书的本地私钥丢失，不得不重新生成整套证书时。此时 fastlane match 官网并没有明确告诉如何应对，但通过 fastlane match 的工作原理，我们还是可以总结出最佳实践的。</p><div class="note note-info"><p>本文假设读者已经在使用 fastlane match，已经会 fastlane match 的基本使用。如果您对 fastlane match 有使用上的问题也欢迎留言交流</p></div><p>下面分两种情况介绍步骤，一种是需要重置证书，一种是需要重置<code>mobileprovision</code>。</p><h1 id="重置证书"><a href="#重置证书" class="headerlink" title="重置证书"></a>重置证书</h1><ol><li>删除 ITC 后台中需要被重置的证书，包括 <code>development</code> 和 <code>distribution</code> 类型的。注意，此步骤不影响已上线的 App 和通过 TestFlight 分发的 App，但是会影响已发布的测试版本 App，插线 Debug 版的和 AdHoc 版本的 App 在联网时将启动即闪退</li><li>删除 ITC 后台中需要被重置证书对应的描述文件。（不是必须的，但如果不删除的话，fastlane match 接下来重新生成的描述文件名将自动带上数字后缀，强迫症患者看了会很难受）</li><li>在本地存放描述文件的文件夹（<code>~/Library/MobileDevice/Provisioning Profiles</code>）中删除对应的描述文件。不是必须的，非洁癖患者可以跳过</li><li>在 fastlane match 所管理的存放证书和描述文件的 git 仓库中，删除需要被重置的证书和描述文件，然后手动 <code>commit</code> 和 <code>push</code></li><li>在本地钥匙串中删除需要被重置的证书，证书和私钥都删除</li><li>执行 <code>fastlane match development</code> (注意不要添加<code>--readonly</code>参数)以重置开发证书，当 fastlane match 检测到 Git 仓库没有副本时将尝试登陆 ITC ，期间会要求输入有权限操作证书的开发者账号信息，如果 ITC 也没有将会重新生成。之后分别使用<code>appstore</code>和<code>adhoc</code> 参数重置 AdHoc 和 AppStore 证书。</li></ol><h1 id="重置描述文件"><a href="#重置描述文件" class="headerlink" title="重置描述文件"></a>重置描述文件</h1><ol><li>删除 ITC 后台中需要被重置的描述文件</li><li>在 fastlane match 所管理的存放证书和描述文件的 git 仓库中，删除需要被重置的描述文件，然后手动 <code>commit</code> 和 <code>push</code></li><li>在本地存放描述文件的文件夹（<code>~/Library/MobileDevice/Provisioning Profiles</code>）中删除需要被重置的描述文件。(不是必须的，非洁癖患者可以跳过)</li><li>执行 <code>fastlane match development</code> (注意不要添加<code>--readonly</code>参数)以重置开发证书，当 fastlane match 检测到 Git 仓库没有副本时将尝试登陆 ITC ，期间会要求输入有权限操作证书的开发者账号信息，如果 ITC 也没有将会重新生成。之后使用<code>adhoc</code> 参数重置 AdHoc 描述文件</li></ol>]]></content>
    
    
    <categories>
      
      <category>开发技巧</category>
      
    </categories>
    
    
    <tags>
      
      <tag>fastlane</tag>
      
      <tag>工具</tag>
      
      <tag>match</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Mach-O 文件结构详解</title>
    <link href="/posts/a58100d4.html"/>
    <url>/posts/a58100d4.html</url>
    
    <content type="html"><![CDATA[<div class="note note-info"><p>Mach-O 文件是通往系统底层的基础，如果要深入理解 App 的启动加载原理，又或是想做深层次的 App 优化，熟悉 Mach-O 是非常必要的。</p></div><p>Mach-O (Mach Object)是一种 iOS 和 MacOS 上的原生二进制文件，类似于 Windows 里面的 PE 和 Linux 里面的 ELF，弄清楚 Mach-O 的结构和加载可以从根本上理解以下技术点：</p><ol><li><a href="https://github.com/facebook/fishhook" target="_blank" rel="noopener">fishhook</a> 原理</li><li>二进制重排加快 App 启动速度</li><li>常量字符串所属模块甚至所属函数的查找</li><li>闪退后，使用 <code>.crash</code> 文件与 <code>dSYM</code> 进行符号恢复</li></ol><p>常接触的 Mach-O 文件有：</p><ol><li>编译产生的目标文件： 目标文件<code>.o</code></li><li>静态/动态库文件： <code>.a</code>、<code>.dylib</code>、<code>.framework</code></li><li>可执行文件：如 .app 包里面的二进制文件</li><li><code>dSYM</code> 文件</li></ol><p><strong>本文将从可执行类型的 64-bit Mach-O 文件来展开</strong>。来源参考：</p><ol><li>头文件：<code>Xcode.app/Contents/Developer/Platforms/iPhoneOS.platform/Developer/SDKs/iPhoneOS.sdk/usr/include/mach-o/loader.h</code></li><li><a href="https://developer.apple.com/library/archive/documentation/Performance/Conceptual/CodeFootprint/Articles/MachOOverview.html" target="_blank" rel="noopener">苹果文档</a></li></ol><h1 id="文件描述"><a href="#文件描述" class="headerlink" title="文件描述"></a>文件描述</h1><h4 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h4><p>Mach-O 可以按照所包含的架构分类，如<code>arm64</code>、<code>armv7</code>、<code>x86_64</code>，一个 Mach-O 文件可以包含不止一个架构，该 Mach-O 将被称为通用二进制格式，即 <code>FAT</code> 二进制 ，通用二进制文件的结构和单架构的只有组织方式上有一些区别，根据苹果官网的一张老图，64-bit Mach-O 的主要组成：<br><img src="a58100d4/16c8a23a6de8960e.png" srcset="/img/loading.gif" alt="Mach-O 结构1"></p><ol><li><code>Header</code>: 位于文件的开始部分，包含二进制的标记信息：类型架构、CPU 类型、文件类型、加载命令的个数等，表明自身的文件属性，方便操作系统内核知道接下来能用什么方式来加载和处理该文件。其中文件类型有目标文件、可执行文件、动态库、动态链接器、<code>dSYM</code> 等。</li><li><code>Load Commands</code>: 紧跟在 <code>Header</code> 的后面，它描述了 <code>segment</code>（段）、符号表、系统动态链接库等的加载，每一个<code>Load Commands</code>都包含有命令类型、占用空间大小、名字等基本信息，不同的数据类型使用不同的加载命令表示。这是和加载（内存映射）相关的区域，操作系统内核为了提高加载效率减少页内碎片，要求每一个段被映射到虚拟内存后所占空间需要按照 <code>PAGESIZE</code> 进行对齐。</li><li><code>Data</code>: 这部分主要存放我们的程序指令和程序所用到的变量、类、方法等，另外还有和装载内存映射相关的 Rebase、Binding、符号表、字符串表等信息。</li></ol><h4 id="整体结构"><a href="#整体结构" class="headerlink" title="整体结构"></a>整体结构</h4><p>整个文件的结构大致为：<br><img src="a58100d4/WX20200518-151703@2x.png" srcset="/img/loading.gif" alt="Mach-O 结构2"></p><h1 id="Mach-O-结构"><a href="#Mach-O-结构" class="headerlink" title="Mach-O 结构"></a>Mach-O 结构</h1><p>和 Mach-O 结构相关的数据结构在<code>&lt;mach-o/loader.h&gt;</code> 和 <code>&lt;mach-o/fat.h&gt;</code>中可以找到，其中 64-bit 的 Header 结构：</p><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">mach_header_64</span> &#123;</span><span class="hljs-keyword">uint32_t</span>magic;      <span class="hljs-comment">/* 魔数，操作系统加载器将根据这个字段决定后续如何识别和加载该二进制 */</span><span class="hljs-keyword">cpu_type_t</span>cputype;<span class="hljs-comment">/* CPU 类型标识*/</span><span class="hljs-keyword">cpu_subtype_t</span>cpusubtype;<span class="hljs-comment">/* CPU 子类型标识 */</span><span class="hljs-keyword">uint32_t</span>filetype;<span class="hljs-comment">/* 文件类型 */</span><span class="hljs-keyword">uint32_t</span>ncmds;<span class="hljs-comment">/* 加载命令的数量 */</span><span class="hljs-keyword">uint32_t</span>sizeofcmds;<span class="hljs-comment">/* Load Commands 在文件中占据空间大小*/</span>    <span class="hljs-comment">/** 描述文件在编译、链接过程中的信息，比较重要的标志位有</span><span class="hljs-comment">     * MH_DYLDLINK: 该文件需要交给 dyld 处理，无法被静态链接</span><span class="hljs-comment">     * MH_PIE: 只在可执行的 Mach-O 中出现，表明操作系统将对主程序启用</span><span class="hljs-comment">     *          地址空间布局随机化</span><span class="hljs-comment">     * MH_TWOLEVEL: 表明当前文件使用了两级命名空间</span><span class="hljs-comment">    */</span><span class="hljs-keyword">uint32_t</span>flags;<span class="hljs-keyword">uint32_t</span>reserved;<span class="hljs-comment">/* reserved */</span>&#125;;</code></pre><p>紧接在 Header 后面的是 Load Commands，其中 <code>LC_SEGMENT_64</code> 数据结构如下</p><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">segment_command_64</span> &#123;</span><span class="hljs-keyword">uint32_t</span>cmd;<span class="hljs-comment">/* 表示将这个64位的段映射到进程地址空间的命令 */</span>    <span class="hljs-comment">/** 一个 command 在文件中所占空间大小。 </span><span class="hljs-comment">     * 操作系统会根据 cmd 的类型来使用相应的 segment_command 数据类型来承接当前 segment，</span><span class="hljs-comment">     * 而 Mach-O 是基于二进制字节流存储的，</span><span class="hljs-comment"> * 所以 cmdsize 其实就是不同的 segment_command 数据类型所占空间的大小</span><span class="hljs-comment">    */</span><span class="hljs-keyword">uint32_t</span>cmdsize;<span class="hljs-keyword">char</span>segname[<span class="hljs-number">16</span>];<span class="hljs-comment">/* 段名字，一般是两个下划线开头后跟大写字母 */</span><span class="hljs-keyword">uint64_t</span>vmaddr;<span class="hljs-comment">/* 段被映射到虚拟内存空间后的起始地址， ASLR 就在这里起作用*/</span><span class="hljs-keyword">uint64_t</span>vmsize;<span class="hljs-comment">/* 段被映射到虚拟内存空间后的大小，这个大小一定是 PAGESIZE 的整数倍 */</span><span class="hljs-keyword">uint64_t</span>fileoff;<span class="hljs-comment">/* 段在文件中的偏移 */</span><span class="hljs-keyword">uint64_t</span>filesize;<span class="hljs-comment">/* 段在文件中的所占空间大小 */</span><span class="hljs-keyword">vm_prot_t</span>maxprot;<span class="hljs-comment">/* 最高级别内存保护标识 */</span><span class="hljs-keyword">vm_prot_t</span>initprot;<span class="hljs-comment">/* 段所在页原始内存保护标识 */</span><span class="hljs-keyword">uint32_t</span>nsects;<span class="hljs-comment">/* section 数量，有的段没有 section */</span><span class="hljs-keyword">uint32_t</span>flags;<span class="hljs-comment">/* flags */</span>&#125;;</code></pre><p>常见的加载命令<code>cmd</code></p><table><thead><tr><th>command 类型</th><th>解释</th></tr></thead><tbody><tr><td><code>LC_SEGMENT_64</code></td><td>将当前段中的数据加载并映射到内存</td></tr><tr><td><code>LC_CODE_SIGNATURE</code></td><td>获取应用的签名信息</td></tr><tr><td><code>LC_UUID</code></td><td>当前文件的唯一标识，可与 dSYM 做匹配用做符号还原</td></tr><tr><td><code>LC_LOAD_DYLIB</code></td><td>需要加载的动态库</td></tr><tr><td><code>LC_LOAD_DYLINKER</code></td><td>用来加载动态库的程序，一般指明 <code>/usr/lib/dyld</code></td></tr><tr><td><code>LC_MAIN</code></td><td>主程序的入口</td></tr><tr><td><code>LC_VERSION_MIN_IPHONEOS</code></td><td>二进制要求的最低操作系统版本</td></tr><tr><td><code>LC_DYLD_INFO_ONLY</code></td><td>描述重定向地址、弱引用地址、懒加载绑定等的偏移值信息</td></tr></tbody></table><p>其中 <code>LC_SEGMENT_64</code> 的子类型有：</p><ul><li><code>__PAGEZERO</code>: 操作系统给当前程序预留的虚拟的“保护空间”，64-bit OS 的该空间大小为 4GB，该区域对于当前程序来说不可读不可写不可执行，即所有代码和数据的加载、执行、写入操作都只能在这 4GB 空间以后的地址上进行，否则将引发异常</li><li><code>__TEXT</code>: 保存程序的代码、OC 相关的类和方法信息、包含在双引号中的常量字符串信息，另外 <code>__TEXT</code> 还预留了动态动态链接库的符号修正信息(__stub)</li><li><code>__DATA</code>: 程序的数据段，可读可写</li><li><code>__LINKEDIT</code>: 包含动态链接相关的所有信息，之前提到的重定向地址、弱引用地址、懒加载绑定信息的索引都存储在这里</li></ul><p>一个 Mach-O 二进制程序的 <code>__DATA</code> 和 <code>__TEXT</code> 段在更细的维度上会被分成不同的 section，这么设计的目的应该是为了提高内存使用效率。在同一个段的同一个 section 中可以使用相同的权限控制，例如对于 <code>__TEXT</code> 段包含代码和常量，是 readonly 的，对于动态链接库来说，操作系统内核在加载这部分数据到内存时，只需要加载一份就可以被所有的进程按需共享；而且由于是只读的，相对于可写可读的<code>__DATA</code>段，操作系统内核不用考虑是否因为数据污染而需要重新加载一个干净的 page；当内核需要收回部分内存时，对于 <code>__TEXT</code> 的页，可以直接标记为丢弃，而不用考虑是否要将变化后的数据保存起来。<br>这也暗示着我们写代码时尽量将不会发生变化的全局变量申明为 <code>const</code> 让其保存在只读区域，能提高内存访问效率，尤其是对于内存区域可能会被共享的动态库</p><h1 id="结构验证"><a href="#结构验证" class="headerlink" title="结构验证"></a>结构验证</h1><p>为了加深理解，下面将通过代码和 MachOView 来探索一个 <a href="https://github.com/Navimark/MachOExploration/blob/master/HelloWorld/HelloWorld" target="_blank" rel="noopener">Demo Mach-O</a> 的 Header 和 Load Commands</p><h4 id="环境准备"><a href="#环境准备" class="headerlink" title="环境准备"></a>环境准备</h4><p>用 Xcode 新建一个 Mac Command Line 程序，名为 MachOExploration，然后再添加一个 iOS Single View App 类型的 target，名为 HelloWorld，然后在 TARGETS -&gt; HelloWorld 的 Build Phases 中新建一个脚本<code>New Run Script Phases</code>，将 Shell 修改为 <code>/usr/bin/python3</code>，然后添加如下脚本</p><pre><code class="hljs python"><span class="hljs-keyword">import</span> os<span class="hljs-keyword">import</span> shutilproduct_dir = os.environ[<span class="hljs-string">"BUILT_PRODUCTS_DIR"</span>]executeable_path = os.environ[<span class="hljs-string">"EXECUTABLE_PATH"</span>] executeable_abs_path = os.path.join(product_dir,executeable_path)executeable_name = os.environ[<span class="hljs-string">"EXECUTABLE_NAME"</span>]project_dir = os.environ[<span class="hljs-string">"PROJECT_DIR"</span>]des_exe_path = os.path.join(project_dir,executeable_name)shutil.copy2(executeable_abs_path, des_exe_path)</code></pre><p>这样运行 HelloWord 工程后，将会有一个二进制文件被复制到工程中，方便后续分析</p><h4 id="读取-Header"><a href="#读取-Header" class="headerlink" title="读取 Header"></a>读取 Header</h4><p>读取文件时，为了合理利用内存，选择使用文件指针来操作文件而不是一次性将文件载入内存：</p><pre><code class="hljs c"><span class="hljs-keyword">uint32_t</span> magic1;NSString *machoFilePath = @<span class="hljs-string">"path/to/mach-o/file"</span>;FILE *fp = fopen(machoFilePath.UTF8String, <span class="hljs-string">"r"</span>);</code></pre><p>由前面的分析可知，主要是基于文件的偏移和大小来得到指定数据，所以定义一个工具函数：</p><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">void</span> *<span class="hljs-title">load_bytes</span><span class="hljs-params">(FILE *fp, <span class="hljs-keyword">int</span> offset, <span class="hljs-keyword">int</span> <span class="hljs-built_in">size</span>)</span> </span><span class="hljs-function"></span>&#123;    <span class="hljs-keyword">void</span> *buf = <span class="hljs-built_in">calloc</span>(<span class="hljs-number">1</span>, <span class="hljs-built_in">size</span>);    <span class="hljs-keyword">if</span>(!buf) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;    &#125;    fseek(fp, offset, SEEK_SET);    fread(buf, <span class="hljs-built_in">size</span>, <span class="hljs-number">1</span>, fp);    <span class="hljs-keyword">return</span> buf;&#125;</code></pre><ul><li>读取 Header<pre><code class="hljs c"><span class="hljs-keyword">uint32_t</span> *magic = load_bytes(fp, <span class="hljs-number">0</span>, <span class="hljs-keyword">sizeof</span>(<span class="hljs-keyword">uint32_t</span>));NSLog(@<span class="hljs-string">"%X"</span>,*magic);<span class="hljs-built_in">free</span>(magic);</code></pre>得到魔数为 <code>FEEDFACF</code>，即 64-bit Mach-O，且不需要大小端转换，接下来使用 <code>struct mach_header_64</code> 类型加载 Header 信息：<pre><code class="hljs objc"><span class="hljs-keyword">void</span> load_header_64(FILE *fp)&#123;    <span class="hljs-keyword">struct</span> mach_header_64 *header = load_bytes(fp, <span class="hljs-number">0</span>, <span class="hljs-keyword">sizeof</span>(<span class="hljs-keyword">struct</span> mach_header_64));    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"magic:%X"</span>,header-&gt;magic);   <span class="hljs-comment">// FEEDFACF</span>    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"cputype:%d"</span>,header-&gt;cputype); <span class="hljs-comment">// 16777228 = CPU_TYPE_ARM64;</span>    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"cpusubtype:%d"</span>,header-&gt;cpusubtype); <span class="hljs-comment">// 0 = CPU_SUBTYPE_VAX_ALL;</span>    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"filetype:%d"</span>,header-&gt;filetype); <span class="hljs-comment">// 2 = MH_EXECUTE;</span>    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"ncmds:%d"</span>,header-&gt;ncmds); <span class="hljs-comment">// 21</span>    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"sizeofcmds:%d"</span>,header-&gt;sizeofcmds); <span class="hljs-comment">// 2704</span><span class="hljs-comment">/// 2097285 = (flags &amp; MH_TWOLEVEL) | (flags &amp; MH_PIE) |</span>    <span class="hljs-comment">///           (flags &amp; MH_DYLDLINK) | (flags &amp; MH_NOUNDEFS)</span>    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"flags:%d"</span>,header-&gt;flags); <span class="hljs-comment">// 2097285</span>    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"reserved:%d"</span>,header-&gt;reserved); <span class="hljs-comment">// 0</span>    free(header);&#125;</code></pre>上述宏定义可以在 <code>iPhoneOS.sdk/usr/include/mach-o/loader.h</code> 和 <code>MacOSX.sdk/usr/include/mach/machine.h</code> 查到<br>将 <a href="https://github.com/Navimark/MachOExploration/blob/master/HelloWorld/HelloWorld" target="_blank" rel="noopener">Demo Mach-O</a> 拖入到 MachOView 中可以验证：<br><img src="a58100d4/header-mach-oviewer.png" srcset="/img/loading.gif" alt="图片"></li></ul><h4 id="读取-Load-Commands"><a href="#读取-Load-Commands" class="headerlink" title="读取 Load Commands"></a>读取 Load Commands</h4><p>Load Commands 紧跟在 Header 的后面，所有的 Load Commands 都“继承”自同一个基结构体，先用这个基结构体去套取数据以得到 load command 的类型，然后选用合适的类型读取完整的数据</p><pre><code class="hljs c"><span class="hljs-comment">// 基结构体</span><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">load_command</span> &#123;</span><span class="hljs-keyword">uint32_t</span> cmd;<span class="hljs-comment">/* type of load command */</span><span class="hljs-keyword">uint32_t</span> cmdsize;<span class="hljs-comment">/* total size of command in bytes */</span>&#125;;</code></pre><p>读取代码</p><pre><code class="hljs objc">uint32_t ncmds = header-&gt;ncmds;<span class="hljs-comment">// 加载 Load Commands</span>uint32_t lc_offset = <span class="hljs-keyword">sizeof</span>(<span class="hljs-keyword">struct</span> mach_header_64);<span class="hljs-keyword">for</span> (<span class="hljs-built_in">NSInteger</span> i = <span class="hljs-number">0</span>; i != ncmds; ++i) &#123;<span class="hljs-keyword">struct</span> load_command *lc_base = load_bytes(fp,lc_offset,<span class="hljs-keyword">sizeof</span>(<span class="hljs-keyword">struct</span> load_command));load_commands_handler(lc_base, fp, lc_offset);lc_offset += lc_base-&gt;cmdsize;free(lc_base);&#125;</code></pre><p>读取到的基结构后，使用完成的结构体套取完整数据：</p><pre><code class="hljs objc"><span class="hljs-keyword">void</span> segment_64_load_handler(FILE *fp, uint32_t offset)&#123;    <span class="hljs-keyword">struct</span> segment_command_64 *sc_64 = load_bytes(fp, offset,                                         <span class="hljs-keyword">sizeof</span>(<span class="hljs-keyword">struct</span> segment_command_64));    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"cmd:%d"</span>,sc_64-&gt;cmd);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"cmdsize:%d"</span>,sc_64-&gt;cmdsize);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"segname:%s"</span>,sc_64-&gt;segname);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"vmaddr:%llu"</span>,sc_64-&gt;vmaddr);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"vmsize:%llu"</span>,sc_64-&gt;vmsize);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"fileoff:%llu"</span>,sc_64-&gt;fileoff);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"filesize:%llu"</span>,sc_64-&gt;filesize);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"maxprot:%d"</span>,sc_64-&gt;maxprot);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"initprot:%d"</span>,sc_64-&gt;initprot);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"nsects:%d"</span>,sc_64-&gt;nsects);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"flags:%d"</span>,sc_64-&gt;flags);    free(sc_64);&#125;<span class="hljs-keyword">void</span> dyld_info_command_handler(FILE *fp, uint32_t offset)&#123;    <span class="hljs-keyword">struct</span> dyld_info_command *dyld_c = load_bytes(fp, offset,                                         <span class="hljs-keyword">sizeof</span>(<span class="hljs-keyword">struct</span> dyld_info_command));<span class="hljs-comment">// 使用 %d 打印会溢出</span>    <span class="hljs-comment">// 2147483682 = (0x22 | LC_REQ_DYLD) = (0x22 | 0x80000000) = LC_DYLD_INFO_ONLY</span>    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"cmd:%@"</span>,@(dyld_c-&gt;cmd));     <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"cmdsize:%d"</span>,dyld_c-&gt;cmdsize);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"rebase_off:%d"</span>,dyld_c-&gt;rebase_off);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"rebase_size:%d"</span>,dyld_c-&gt;rebase_size);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"bind_off:%d"</span>,dyld_c-&gt;bind_off);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"bind_size:%d"</span>,dyld_c-&gt;bind_size);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"weak_bind_off:%d"</span>,dyld_c-&gt;weak_bind_off);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"weak_bind_size:%d"</span>,dyld_c-&gt;weak_bind_size);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"lazy_bind_off:%d"</span>,dyld_c-&gt;lazy_bind_off);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"lazy_bind_size:%d"</span>,dyld_c-&gt;lazy_bind_size);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"export_off:%d"</span>,dyld_c-&gt;export_off);    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"export_size:%d"</span>,dyld_c-&gt;export_size);free(dyld_c);&#125;<span class="hljs-keyword">void</span> load_commands_handler(<span class="hljs-keyword">struct</span> load_command *lc_base, FILE *fp, uint32_t offset)&#123;    <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"--------------------------------"</span>);    uint32_t cmd = lc_base-&gt;cmd;    <span class="hljs-keyword">switch</span> (cmd) &#123;        <span class="hljs-keyword">case</span> LC_SEGMENT_64:            segment_64_load_handler(fp, offset);            <span class="hljs-keyword">break</span>;        <span class="hljs-keyword">case</span> LC_DYLD_INFO_ONLY:            dyld_info_command_handler(fp, offset);            <span class="hljs-keyword">break</span>;        <span class="hljs-keyword">default</span>:            <span class="hljs-keyword">break</span>;    &#125;&#125;</code></pre><p>完整代码<a href="https://github.com/Navimark/MachOExploration/blob/master/MachOExploration/main.m" target="_blank" rel="noopener">在这里</a>，后续基本上都是这种模式，根据偏移值和定义来获取数据，这里不再继续解析了。</p><h1 id="一点感触"><a href="#一点感触" class="headerlink" title="一点感触"></a>一点感触</h1><p>读取 Mach-O 文件的精髓是偏移 + <code>struct</code>，<code>struct</code> 真是访问字节流内容的神器：</p><ul><li>在探索 oc 底层结构时，打印 <code>Class</code> 类型的 <code>isa</code> 指针时，由于某些原因（暂时未知）不能直接 lldb po 出来，这时可以<a href="[iOS底层原理之六：isa指针实现之ISA_MASK](https://www.jianshu.com/p/8275a6f15bfc)">定义一个和类对象一样结构的结构体来套取 <code>isa</code> 指针</a>。</li><li><a href="https://navimark.github.io/2020/05/16/cong-hui-bian-li-jie-block-de-nei-cun-jie-gou/" target="_blank" rel="noopener">oc 的 <code>block</code> 转成 c++ 代码后</a>，有一个代表 block 的基本结构的类型 <code>struct __block_impl</code>:<pre><code class="hljs c++"><span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">block_impl</span> &#123;</span><span class="hljs-keyword">void</span> *isa;<span class="hljs-keyword">int</span> Flags;<span class="hljs-keyword">int</span> Reserved;<span class="hljs-keyword">void</span> *FuncPtr;&#125;;</code></pre>还有代表当前完整内存结构的 block <code>struct __main_block_impl_0</code>(名字可能不一样)，这两个是包含关系：<pre><code class="hljs c++"><span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">main_block_impl_0</span> &#123;</span><span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">block_impl</span> <span class="hljs-title">impl</span>;</span><span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">main_block_desc_0</span>* <span class="hljs-title">Desc</span>;</span>__Block_byref_age_0 *age; <span class="hljs-comment">// block 捕获的变量，封装成了结构体</span>__main_block_impl_0(<span class="hljs-keyword">void</span> *fp, struct __main_block_desc_0 *desc,                        __Block_byref_age_0 *_age, <span class="hljs-keyword">int</span> flags=<span class="hljs-number">0</span>) :                                            age(_age-&gt;__forwarding) &#123;impl.isa = &amp;_NSConcreteStackBlock;impl.Flags = flags;impl.FuncPtr = fp;Desc = desc;&#125;&#125;;</code></pre>对于一块具体的 block 内存来说无论是通过 <code>struct __block_impl *</code> 还是 <code>struct __main_block_impl_0 *</code> 都是可以合法访问的，区别是当涉及到基本 block 属性的操作时使用 <code>struct __block_impl *</code> ，当涉及到具体 block 属性操作时使用 <code>struct __main_block_impl_0 *</code>，最大化地精炼和减少额外开销</li><li><a href="https://mp.weixin.qq.com/s/9-o5iA1-lQ3s1iwIBcXLuQ" target="_blank" rel="noopener">一个基于汇编的中心重定向框架</a>中讲到，为了在汇编层面上精确无误地访问之前构建的两页内存中的数据，设计了特殊的结构体，通过该结构体套取特定偏移位置的数据，设计非常巧妙</li></ul>]]></content>
    
    
    <categories>
      
      <category>Mach-O</category>
      
    </categories>
    
    
    <tags>
      
      <tag>底层</tag>
      
      <tag>iOS</tag>
      
      <tag>Objective-C</tag>
      
      <tag>Mach-O</tag>
      
      <tag>数据结构</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>从汇编理解 Block 的内存结构</title>
    <link href="/posts/a3f46529.html"/>
    <url>/posts/a3f46529.html</url>
    
    <content type="html"><![CDATA[<p>❓ 在断点调试 iOS 程序碰到 block 作为函数的形参时，如果想知道该 block 本身的函数签名信息和函数体地址时，有哪些办法？<br>🤪 当然是在源码里面直接查看 block 的声明和调用了！<br>❗️ 但如果源码不可见呢？在分析第三方闭源库或友商 App 的某些逻辑实现时，就只有汇编代码可用</p><p>☕️ 本文将通过汇编代码入手探讨 block 的内存结构，并尝试还原 block 的函数签名信息和函数体真实地址</p><h2 id="环境准备"><a href="#环境准备" class="headerlink" title="环境准备"></a>环境准备</h2><div class="note note-warning"><p>如果拿真实案例来做分析，势必会有一系列繁琐的前期准备步骤，而且一时也想不起来有哪些友商 App 刚好可以做 demo，所以本文将使用本地环境做模拟，并直接从最关键的步骤开始</p></div><ol><li><p>打开 Xcode，新建一个 <code>Single View App</code>，将下面的代码贴入 <code>ViewController.m</code> 中，准备好证书和 64bit 真机。</p><pre><code class="hljs objc"><span class="hljs-meta">#import <span class="hljs-meta-string">"ViewController.h"</span></span><span class="hljs-keyword">typedef</span> <span class="hljs-keyword">void</span>(^HHBlock)(<span class="hljs-built_in">NSString</span> *,<span class="hljs-built_in">NSInteger</span> );<span class="hljs-class"><span class="hljs-keyword">@interface</span> <span class="hljs-title">ViewController</span> ()</span><span class="hljs-keyword">@end</span><span class="hljs-class"><span class="hljs-keyword">@implementation</span> <span class="hljs-title">ViewController</span></span>- (<span class="hljs-keyword">void</span>)viewDidLoad &#123;    [<span class="hljs-keyword">super</span> viewDidLoad];    <span class="hljs-keyword">void</span> (^testBlock)(<span class="hljs-built_in">NSString</span> *, <span class="hljs-built_in">NSInteger</span> ) = ^(<span class="hljs-built_in">NSString</span> *var1, <span class="hljs-built_in">NSInteger</span> var2)&#123;        <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@"var1 = %@, var2 = %@"</span>, var1, @(var2));    &#125;;    [<span class="hljs-keyword">self</span> doBlockTest:testBlock];&#125;- (<span class="hljs-keyword">void</span>)doBlockTest:(HHBlock)block&#123;    block(<span class="hljs-string">@"v1"</span>,<span class="hljs-number">10086</span>); &#125;<span class="hljs-keyword">@end</span></code></pre></li><li><p>为了尽量模拟真实环境也为了去掉汇编中不必要的指令，在 Xcode 的 Scheme 中将 <code>Run -&gt; Build Configuration</code> 修改为 Release ；然后在 <code>[self doBlockTest:testBlock];</code> 这行下断点</p></li><li><p>点 <code>Run</code>，等运行到断点位置后，勾选 Xcode 菜单 <code>Debug -&gt; Debug Workflow -&gt; Always Show Disassembly</code> 打开汇编指令视图：<br><img src="a3f46529/1.png" srcset="/img/loading.gif" alt=""></p></li></ol><h2 id="调用前的分析"><a href="#调用前的分析" class="headerlink" title="调用前的分析"></a>调用前的分析</h2><blockquote><p>从现在开始，我们假装忘记了刚刚写的源码，尝试从汇编代码中得到 block 的基本信息。</p></blockquote><ol><li><p>从第 14 行开始分析：<br>L14. <code>nop</code>: 空指令，什么也不做，猜测是和内存对齐有关<br>L15. <code>ldr x1, #0x3260</code>: 将 <code>#0x3260</code> 指向的内存数据加载到寄存器 <code>x1</code> 中<br>L16. <code>adr x2, #0x1dfc</code>: 将 <code>#0x1dfc</code> 指向的内存数据加载到寄存器 <code>x2</code> 中<br>L17. 同第 14 行<br>L18. <code>mov x0, x19</code>: 将 19 号寄存器的值复制到 0 号寄存器<br>L19. <code>bl</code>: 调用 <code>0x100582540</code> 处的函数，即 <code>objc_msgSend</code> 的调用</p></li><li><p>按住 <code>Control</code> 键，通过点 <code>Step info</code> 单步执行到第 19 行。<code>objc_msgSend</code> 原型为<code>objc_msgSend(id self, SEL op,...)</code>，本例中有三个参数，按照约定这三个参数将依次放在<code>x0</code>、<code>x1</code>、<code>x2</code> 中。所以 <code>x0</code> 为 <code>ViewController</code> 实例，<code>x1</code> 为 selector，<code>x2</code> 为 block 结构体指针。读取寄存器和打印 <code>x0</code> 可验证：<br><img src="a3f46529/2.png" srcset="/img/loading.gif" alt=""><br><img src="a3f46529/3.png" srcset="/img/loading.gif" alt=""></p></li></ol><h2 id="block-类型形参的分析"><a href="#block-类型形参的分析" class="headerlink" title="block 类型形参的分析"></a>block 类型形参的分析</h2><div class="note note-danger"><p>前面的更多是汇编基础的回顾，现在即将进入重点</p></div><ol><li><p>点 <code>Step over</code> 进入到真正被调用函数，如图：<br><img src="a3f46529/4.png" srcset="/img/loading.gif" alt=""><br>按照刚刚的分析，<code>x2</code> 里面就是这个 block 形参，直接 <code>po</code>该形参是没有函数体地址和函数签名等信息的：<br><img src="a3f46529/5.png" srcset="/img/loading.gif" alt=""></p></li><li><p>尝试还原 block 信息，按行分析：<br>L3. <code>ldr x3, [x2, #0x10]</code>: 将 <code>x2</code> 指向的内容加上 16 个字节偏移后的地址，加载到<code>x3</code>中<br>L4. <code>adr x1, #0x1d94</code>: 将 <code>#0x1d94</code> 指向的内容加载到 <code>x1</code><br>L5. 同上<br>L6. <code>mov w2, #0x2766</code>: 将 <code>#0x2766</code> 复制到 <code>w2</code> 的低 32 位上。<code>0x2766</code> 也就是十进制的 <code>10086</code><br>L7. <code>br x3</code>: 跳转到 <code>x3</code> 指向的地址上执行</p></li><li><p>根据上下文，第 2 行中的 <code>x2</code> 中就是 block 形参指针（<code>0x0000000100584090</code>），在第 3 行中，取了该指针指向内容并偏移 <code>0x10</code> 处的地址 P 赋值给了 <code>x3</code>，在第 7 行中执行了 <code>x3</code> 指向的内容（也是一个地址），说明 P 是一个函数（函数指针）可被执行，这正是 block 的特性。参考 <a href="https://navimark.github.io/2020/05/08/you-block-de-nei-cun-bu-ju-yan-sheng-er-lai-de-mian-shi-ti/" target="_blank" rel="noopener">由 block 的内存布局衍生而来的面试题</a></p></li><li><p>根据 <a href="http://clang.llvm.org/docs/Block-ABI-Apple.html" target="_blank" rel="noopener">llvm 官网对 block 实施标准的描述</a>：</p><pre><code class="hljs objc"><span class="hljs-keyword">struct</span> Block_literal_1 &#123;    <span class="hljs-keyword">void</span> *isa; <span class="hljs-comment">// initialized to &amp;_NSConcreteStackBlock or &amp;_NSConcreteGlobalBlock</span>    <span class="hljs-keyword">int</span> flags;    <span class="hljs-keyword">int</span> reserved;    <span class="hljs-keyword">void</span> (*invoke)(<span class="hljs-keyword">void</span> *, ...);    <span class="hljs-keyword">struct</span> Block_descriptor_1 &#123;        <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> <span class="hljs-keyword">int</span> reserved;         <span class="hljs-comment">// NULL</span>        <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> <span class="hljs-keyword">int</span> size;         <span class="hljs-comment">// sizeof(struct Block_literal_1)</span>        <span class="hljs-comment">// optional helper functions</span>        <span class="hljs-keyword">void</span> (*copy_helper)(<span class="hljs-keyword">void</span> *dst, <span class="hljs-keyword">void</span> *src);     <span class="hljs-comment">// IFF (1&lt;&lt;25)</span>        <span class="hljs-keyword">void</span> (*dispose_helper)(<span class="hljs-keyword">void</span> *src);             <span class="hljs-comment">// IFF (1&lt;&lt;25)</span>        <span class="hljs-comment">// required ABI.2010.3.16</span>        <span class="hljs-keyword">const</span> <span class="hljs-keyword">char</span> *signature;                         <span class="hljs-comment">// IFF (1&lt;&lt;30)</span>    &#125; *descriptor;    <span class="hljs-comment">// imported variables</span>&#125;;</code></pre><p>block 结构体指针偏移 <code>sizeof(void *) + sizeof(int) + sizeof(reserved)</code> 后就是真正函数体 <code>invoke</code> 的地址。在 64bit 系统上指针类型和 <code>int</code> 类型分别占用 8 个字节和 4 个字节，考虑到结构体内存对齐，真正的函数体的偏移量为： 8 + 8 = 0x10，即 16 个字节，<code>invoke</code> 的地址为： <code>结构体指针地址 + offset(16 字节)</code> 。<br>接下来我们读取这个函数体地址，首先打印 block 结构体指针指向的内容：<br><img src="a3f46529/6.png" srcset="/img/loading.gif" alt=""></p><p>结构体内容开始于 <code>0x00000001b365a288</code>，函数体 <code>invoke</code> 地址开始于 16 字节后，占用 8 个字节，所以其地址为：<code>0x00000001005822b4</code>，我们在这里下一个断点：<code>br s -a 0x00000001005822b4</code>，等会验证下是不是刚好断在 block 的实现里面</p></li><li><p>获取 block 的函数签名<br>函数签名在 <code>Block_descriptor_1</code> 类型结构体 <code>descriptor</code> 的 <code>signature</code> 成员变量上，我们目标是通过 <code>signature</code> 得到 <code>NSMethodSignature</code> 实例，进而得到 <code>invoke</code> 详细的函数签名信息。<code>descriptor</code> 的偏移量： <code>offset(*invoke) + sizeof(*invoke)</code>，所以 <code>descriptor</code> 的地址为 <code>0x0000000100584070</code>。另外根据文档，并不是所有 block 都存在这个 <code>signature</code> 变量，需要通过 <code>flags</code> 与 block 中定义的枚举掩码进行 <code>&amp;</code> 操作来判断，枚举掩码定义：</p><pre><code class="hljs c"><span class="hljs-keyword">enum</span> &#123;    <span class="hljs-comment">// Set to true on blocks that have captures (and thus are not true</span>    <span class="hljs-comment">// global blocks) but are known not to escape for various other</span>    <span class="hljs-comment">// reasons. For backward compatibility with old runtimes, whenever</span>    <span class="hljs-comment">// BLOCK_IS_NOESCAPE is set, BLOCK_IS_GLOBAL is set too. Copying a</span>    <span class="hljs-comment">// non-escaping block returns the original block and releasing such a</span>    <span class="hljs-comment">// block is a no-op, which is exactly how global blocks are handled.</span>    BLOCK_IS_NOESCAPE      =  (<span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">23</span>),    BLOCK_HAS_COPY_DISPOSE =  (<span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">25</span>),    BLOCK_HAS_CTOR =          (<span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">26</span>), <span class="hljs-comment">// helpers have C++ code</span>    BLOCK_IS_GLOBAL =         (<span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">28</span>),    BLOCK_HAS_STRET =         (<span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">29</span>), <span class="hljs-comment">// IFF BLOCK_HAS_SIGNATURE</span>    BLOCK_HAS_SIGNATURE =     (<span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">30</span>),&#125;;</code></pre><p>按照刚刚的方法，尝试获取 <code>flags</code> 的值：<br><img src="a3f46529/7.png" srcset="/img/loading.gif" alt=""></p><p><code>flags</code> 的值为 <code>0x50000000</code>，判断后发现存在 <code>signature</code>:<br><img src="a3f46529/8.png" srcset="/img/loading.gif" alt=""></p><p><code>signature</code> 近在咫尺！但是仔细看 block 文档的定义，排在 <code>signature</code> 前面的两个函数指针 <code>copy_helper</code> 和 <code>dispose_helper</code> 有这句注释：<code>optional helper functions</code>，看来还需要判断这两个指针是否存在：<br><img src="a3f46529/9.png" srcset="/img/loading.gif" alt=""></p><p>嗯，不存在，所以 <code>signature</code> 的偏移量:<code>offset(long int) + offset(long int)</code>，长度 <code>sizeof(char *)</code>，接着打印 <code>descriptor</code> 的内容：<br><img src="a3f46529/10.png" srcset="/img/loading.gif" alt=""></p><p>所以 <code>signature</code> 的值为 <code>0x0000000100583382</code>，打印看看是什么:<br><img src="a3f46529/11.png" srcset="/img/loading.gif" alt=""></p><p>眼熟的 Type Encodings 字符串，转换成 <code>NSMethodSignature</code> 的实例看看：<br><img src="a3f46529/12.png" srcset="/img/loading.gif" alt=""></p><p>这个 block 函数体没有返回值（<code>v</code>-&gt;<code>void</code>），接收 3 个参数，第一个是 block ——这是所有 block 函数体的潜规则（将上述源码编译为 C++ 的就能知道），后续两个的参数依次为：<code>@</code>(<code>NSString *</code>) 和 <code>q</code>(<code>long long</code>)，和源码一致。</p></li><li><p>函数体地址正确性验证<br>还记得第 4 步在 <code>0x00000001005822b4</code> 设置的断点吗？点 <code>Continue</code> 或 lldb 输入 <code>c</code>，让程序继续执行，结果在 <code>0x00000001005822b4</code> 处被断住了：<img src="a3f46529/13.png" srcset="/img/loading.gif" alt=""><br>而且断住的位置在 <code>ViewController.m:19</code>，查看源码发现刚好就是 block 的实现：<img src="a3f46529/14.png" srcset="/img/loading.gif" alt=""></p></li></ol><p>PS：本人汇编初学，若发现理解不到位或者错误的地方，望指教！</p><h2 id="Ref"><a href="#Ref" class="headerlink" title="Ref"></a>Ref</h2><ul><li><a href="https://juejin.im/post/5cadeda55188251ad87b0eed" target="_blank" rel="noopener">arm64 架构之入栈/出栈操作</a></li><li><a href="http://clang.llvm.org/docs/Block-ABI-Apple.html" target="_blank" rel="noopener">Clang 11 documentation - Block Implementation Specification</a></li><li><a href="https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/ObjCRuntimeGuide/Articles/ocrtTypeEncodings.html" target="_blank" rel="noopener">Type Encodings 对照表</a></li></ul>]]></content>
    
    
    <categories>
      
      <category>汇编</category>
      
    </categories>
    
    
    <tags>
      
      <tag>iOS</tag>
      
      <tag>LLDB</tag>
      
      <tag>汇编</tag>
      
      <tag>block</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>由 block 的内存布局衍生而来的面试题</title>
    <link href="/posts/80ce330f.html"/>
    <url>/posts/80ce330f.html</url>
    
    <content type="html"><![CDATA[<p>今天在群里看到大佬们在讨论一个面试题，问如下代码在 32bit 和 64bit 系统上分别报什么错误：</p><pre><code class="hljs objc"><span class="hljs-meta">#import <span class="hljs-meta-string">&lt;Foundation/Foundation.h&gt;</span></span><span class="hljs-keyword">int</span> main()&#123;    <span class="hljs-keyword">void</span> (^block)(<span class="hljs-keyword">void</span>) = <span class="hljs-literal">nil</span>;    block();    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;&#125;</code></pre><p>虽然有大佬一下子说出了答案，但我仍然一脸懵逼，后来经人提醒，这个考察 block 在内存中的结构，于是赶紧做了如下实验终于弄懂了为什么。</p><ul><li><p>实验<br>将以上代码保存为 <code>block_test.m</code> ，在命令行编译成 C++:<code>clang -rewrite-objc -fobjc-arc block_test.m</code>，打开同一目录下生成的<code>block_test.cpp</code>文件，截取如下关键代码：</p><pre><code class="hljs c++"><span class="hljs-comment">// 注释①</span><span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">block_impl</span> &#123;</span>  <span class="hljs-keyword">void</span> *isa;  <span class="hljs-keyword">int</span> Flags;  <span class="hljs-keyword">int</span> Reserved;  <span class="hljs-keyword">void</span> *FuncPtr;&#125;;<span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><span class="hljs-function"></span>&#123;    <span class="hljs-keyword">void</span> (*block)(<span class="hljs-keyword">void</span>) = __null;    <span class="hljs-comment">// 注释 ②</span>    ((<span class="hljs-keyword">void</span> (*)(__block_impl *))((__block_impl *)block)-&gt;FuncPtr)((__block_impl *)block);    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;&#125;</code></pre></li><li><p>解释：</p><ol><li>注释①所在的结构体是 block 在内存中的数据结构。</li><li>注释②是一个函数调用语句，可以简化成：<code>((__block_impl *)block)-&gt;FuncPtr(block)</code>，即调用 <code>__block_impl</code> 类型指针指向的结构体中 <code>FuncPtr</code> 指向的函数，由于该结构体指针指向的内容为 null，因此 <code>FuncPtr</code>指向的位置显然不是一个函数，所以该指针指向的位置会发生一个<code>EXC_BAD_ACCESS</code> 错误。</li><li><code>FuncPtr</code> 指向的位置是多少呢？根据上述<code>__block_impl</code>中各成员变量的排列可知 <code>FuncPtr</code> 的偏移是 <code>sizeof(void *) + sizeof(int) + sizeof(int)</code>，另外还需要考虑结构体内存对齐，参考<a href="https://www.jianshu.com/p/3294668e2d8c" target="_blank" rel="noopener">这个链接</a>，所以在 32bit 系统中，偏移：<code>4 + 4 + 4 = 0x0C</code>；64bit 系统中，偏移：<code>8 + 8 = 0x10</code></li></ol></li><li><p>结论：</p><ul><li>答案：在 32bit 系统中报错：<code>EXC_BAD_ACCESS(address = 0x0C)</code>，在 64bit 系统中报错<code>EXC_BAD_ACCESS(address = 0x10)</code></li><li>block 本质是一个结构体，在探究 block 相关的问题时，转换成 <code>C++</code> 代码后往往就可以一目了然，比如<a href="https://www.jianshu.com/p/4e9d3ef9a199" target="_blank" rel="noopener">weakSelf 为什么可以解决循环引用</a></li></ul></li></ul>]]></content>
    
    
    <categories>
      
      <category>底层</category>
      
    </categories>
    
    
    <tags>
      
      <tag>iOS</tag>
      
      <tag>Objective-C</tag>
      
      <tag>汇编</tag>
      
      <tag>block</tag>
      
      <tag>面试</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>如何快速更换 App icon</title>
    <link href="/posts/c097b069.html"/>
    <url>/posts/c097b069.html</url>
    
    <content type="html"><![CDATA[<blockquote><p>每逢重大节日，App icon 就要跟一波“潮流”做一次更换，节日过后再换回普通。如何保证这两次切换流程丝滑顺畅呢？</p></blockquote><p>应用内需要更换的 icon 包括两处，一个是 App 主 icon，默认放在 <code>xcassets</code> 里面，另一个就是 App 内部页面所使用的 icon。</p><h1 id="App-主-icon-更换"><a href="#App-主-icon-更换" class="headerlink" title="App 主 icon 更换"></a>App 主 icon 更换</h1><p>苹果这边需要的 icon 实在太多了，如果像我们 App 一样支持 iPad 那么大大小小的 icon 就需要 18 张，就算让设计师同学给到所有需要的尺寸我们自己在 <code>.xcassets</code> 一一对应起来也是超级麻烦，如果我们只需要提供一张高清图（1024x1024 pixel）剩下的能通过工具自动对应起来该多好啊！</p><p>研究<code>xcassets</code>结构后发现，AppIcon 类型的图片是一个后缀名为 <code>appiconset</code> 的文件夹，该文件夹里面除了有 APP 需要的各种尺寸的 <code>png</code> 图片外，还有一个 <code>Contents.json</code> 文件，形如：</p><pre><code class="hljs json">&#123;  <span class="hljs-attr">"images"</span> : [    &#123;      <span class="hljs-attr">"size"</span> : <span class="hljs-string">"20x20"</span>,      <span class="hljs-attr">"idiom"</span> : <span class="hljs-string">"iphone"</span>,      <span class="hljs-attr">"filename"</span> : <span class="hljs-string">"IOS_40-2.png"</span>,      <span class="hljs-attr">"scale"</span> : <span class="hljs-string">"2x"</span>    &#125;,    &#123;      <span class="hljs-attr">"size"</span> : <span class="hljs-string">"83.5x83.5"</span>,      <span class="hljs-attr">"idiom"</span> : <span class="hljs-string">"ipad"</span>,      <span class="hljs-attr">"filename"</span> : <span class="hljs-string">"IOS_167.png"</span>,      <span class="hljs-attr">"scale"</span> : <span class="hljs-string">"2x"</span>    &#125;,    &#123;      <span class="hljs-attr">"size"</span> : <span class="hljs-string">"1024x1024"</span>,      <span class="hljs-attr">"idiom"</span> : <span class="hljs-string">"ios-marketing"</span>,      <span class="hljs-attr">"filename"</span> : <span class="hljs-string">"IOS_1024.png"</span>,      <span class="hljs-attr">"scale"</span> : <span class="hljs-string">"1x"</span>    &#125;  ],  <span class="hljs-attr">"info"</span> : &#123;    <span class="hljs-attr">"version"</span> : <span class="hljs-number">1</span>,    <span class="hljs-attr">"author"</span> : <span class="hljs-string">"xcode"</span>  &#125;&#125;</code></pre><p>描述了了各种尺寸的图片如何与文件夹中的 <code>png</code> 图片对应，我们按照此规律便可以写一个更换 AppIcon 的工具。</p><p>之前确实听说过有自动生成这种 icon 的工具 App 但我没有使用过，要为如此一个小功能下载一个 App 我觉得太不环保了。还是自己写一个脚本实现比较低碳。下面是 python 程序和注释</p><pre><code class="hljs python"><span class="hljs-comment"># !/usr/local/bin/python3</span><span class="hljs-comment"># _*_ coding:utf-8 _*_</span>__doc__=<span class="hljs-string">"""</span><span class="hljs-string">    输入：一个 1024*1024 的 png 图片</span><span class="hljs-string">    输出： AppIconxxxxx.appiconset 目录，包含 iPhone 和 iPad 所需的 App Icons</span><span class="hljs-string">"""</span><span class="hljs-keyword">import</span> os,sys<span class="hljs-keyword">import</span> imghdr<span class="hljs-keyword">import</span> json<span class="hljs-keyword">import</span> random,shutil<span class="hljs-keyword">from</span> PIL <span class="hljs-keyword">import</span> Image,ImageFile<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FileSet</span>:</span>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">__init__</span><span class="hljs-params">(self,filename,scale)</span>:</span>        self.filename = filename        self.scale = scale    <span class="hljs-meta">    @classmethod</span>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">fileset</span><span class="hljs-params">(cls,scale,size,prefix)</span>:</span>        filename = <span class="hljs-string">"&#123;&#125;.&#123;&#125;.&#123;&#125;.png"</span>.format(size,scale,prefix)        file_set = FileSet(filename,scale)        <span class="hljs-keyword">return</span> file_set<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ImageSet</span>:</span>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">__init__</span><span class="hljs-params">(self,size,idiom,filesets)</span>:</span>        self.size = size    <span class="hljs-comment"># 单边</span>        self.idiom = idiom          self.filesets = filesets    <span class="hljs-comment"># 数组，包含文件名，一个 size 可能有多个 scale，所有会有多个文件 set</span>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">json_desc</span><span class="hljs-params">(self)</span>:</span>        descs = []        <span class="hljs-keyword">for</span> fileset <span class="hljs-keyword">in</span> self.filesets:            json_dict = &#123;<span class="hljs-string">"size"</span>:<span class="hljs-string">"&#123;&#125;x&#123;&#125;"</span>.format(self.size,self.size),                        <span class="hljs-string">"idiom"</span>:self.idiom,                        <span class="hljs-string">"filename"</span>:fileset.filename,                        <span class="hljs-string">"scale"</span>:<span class="hljs-string">"&#123;&#125;x"</span>.format(fileset.scale)&#125;            descs.append(json_dict)        <span class="hljs-keyword">return</span> descs<span class="hljs-meta">    @classmethod</span>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">iPhone_set</span><span class="hljs-params">(cls,size,filesets)</span>:</span>        <span class="hljs-keyword">return</span> ImageSet(size,<span class="hljs-string">'iphone'</span>,filesets)<span class="hljs-meta">    @classmethod</span>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">iPad_set</span><span class="hljs-params">(cls,size,filesets)</span>:</span>        <span class="hljs-keyword">return</span> ImageSet(size,<span class="hljs-string">'ipad'</span>,filesets)<span class="hljs-meta">    @classmethod</span>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">market_set</span><span class="hljs-params">(cls,file_prefix=<span class="hljs-string">''</span>)</span>:</span>        size = <span class="hljs-number">1024</span>        <span class="hljs-keyword">return</span> ImageSet(size,idiom=<span class="hljs-string">'ios-marketing'</span>,filesets=[FileSet.fileset(<span class="hljs-number">1</span>,size,file_prefix)])<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_img_sets</span><span class="hljs-params">(iPad=False,iPhone=False,file_prefix=<span class="hljs-string">''</span>)</span>:</span>    img_sets = []    <span class="hljs-keyword">if</span> iPad:        <span class="hljs-keyword">for</span> size <span class="hljs-keyword">in</span> [<span class="hljs-number">20</span>,<span class="hljs-number">29</span>,<span class="hljs-number">40</span>,<span class="hljs-number">76</span>]:            file_sets = [FileSet.fileset(<span class="hljs-number">2</span>,size,file_prefix),FileSet.fileset(<span class="hljs-number">1</span>,size,file_prefix)]            one_set = ImageSet.iPad_set(size,file_sets)            img_sets.append(one_set)        img_sets.append(ImageSet.iPad_set(<span class="hljs-number">83.5</span>,[FileSet.fileset(<span class="hljs-number">2</span>,<span class="hljs-number">83.5</span>,file_prefix)]))            <span class="hljs-keyword">if</span> iPhone:        <span class="hljs-keyword">for</span> size <span class="hljs-keyword">in</span> [<span class="hljs-number">20</span>,<span class="hljs-number">29</span>,<span class="hljs-number">40</span>,<span class="hljs-number">60</span>]:            file_sets = [FileSet.fileset(<span class="hljs-number">2</span>,size,file_prefix),FileSet.fileset(<span class="hljs-number">3</span>,size,file_prefix)]            one_set = ImageSet.iPhone_set(size,file_sets)            img_sets.append(one_set)    img_sets.append(ImageSet.market_set(file_prefix))    <span class="hljs-keyword">return</span> img_sets<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">create_appicon_set</span><span class="hljs-params">(imgobj,t_path=<span class="hljs-string">''</span>,iPad=False,iPhone=True)</span>:</span>    rand_str = str(random.randint(<span class="hljs-number">20000</span>,<span class="hljs-number">2147483648</span>))    t_folder_path = os.path.join(t_path,<span class="hljs-string">"AppIcon"</span>+ rand_str +<span class="hljs-string">".appiconset"</span>)    os.makedirs(t_folder_path)    img_sets = get_img_sets(iPad=iPad,iPhone=iPhone,file_prefix=rand_str)    contents = &#123;<span class="hljs-string">"info"</span>:&#123;<span class="hljs-string">"version"</span>:<span class="hljs-number">1</span>,<span class="hljs-string">"author"</span>:<span class="hljs-string">"xcode"</span>&#125;&#125;    images = []    <span class="hljs-keyword">for</span> single_set <span class="hljs-keyword">in</span> img_sets:        <span class="hljs-keyword">for</span> fileset <span class="hljs-keyword">in</span> single_set.filesets:            scale_size = (int(single_set.size * fileset.scale), int(single_set.size * fileset.scale))            img_obj = imgobj.resize(scale_size,Image.ANTIALIAS)            real_path = os.path.join(t_folder_path,fileset.filename)            img_obj.save(real_path)            print(<span class="hljs-string">"保存文件&#123;&#125;,\t路径:&#123;&#125;"</span>.format(scale_size,real_path))        images += single_set.json_desc()    contents[<span class="hljs-string">"images"</span>] = images    <span class="hljs-keyword">with</span> open(os.path.join(t_folder_path,<span class="hljs-string">"Contents.json"</span>),<span class="hljs-string">"w"</span>) <span class="hljs-keyword">as</span> wf:        wf.write(json.dumps(contents,indent=<span class="hljs-number">4</span>))<span class="hljs-keyword">if</span> __name__ == <span class="hljs-string">'__main__'</span>:    argvs = sys.argv[<span class="hljs-number">1</span>:] <span class="hljs-keyword">if</span> len(sys.argv) == <span class="hljs-number">3</span> <span class="hljs-keyword">else</span> <span class="hljs-literal">None</span>    <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> argvs:        print(<span class="hljs-string">"Fatal: 要求两个参数，第一个是图片路径，第二个是目标目录"</span>)        sys.exit(<span class="hljs-number">-1</span>)    o_img_path,t_path = argvs        <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> os.path.isfile(o_img_path) <span class="hljs-keyword">or</span> <span class="hljs-keyword">not</span> imghdr.what(o_img_path) <span class="hljs-keyword">in</span> [<span class="hljs-string">'png'</span>]:        print(<span class="hljs-string">"Fatal: 图片路径不存在或者非 png 格式图片"</span>)        sys.exit(<span class="hljs-number">-1</span>)    <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> os.path.isdir(t_path):        print(<span class="hljs-string">"Fatal: 目标路径不存在或者非目录"</span>)        sys.exit(<span class="hljs-number">-1</span>)    o_img = Image.open(o_img_path)    <span class="hljs-keyword">if</span> (<span class="hljs-number">1024</span>,<span class="hljs-number">1024</span>) != o_img.size :        print(<span class="hljs-string">"Fatal: 图片非 1024x1024 pixel 尺寸"</span>)        sys.exit(<span class="hljs-number">-1</span>)<span class="hljs-string">"""</span><span class="hljs-string">碰到设计师犯糊，将肉眼看不见但确实带有 Alpha 通道的图片提供给我们，直到我们在提交 App Store 的那一刻苹果报错说带有 Alpha 通道，然后我们又要重新走一遍流程🐶。</span><span class="hljs-string">下面会检查该 1024*1024 的图片是否存在 alpha 通道，若存在，移除之</span><span class="hljs-string">"""</span>    <span class="hljs-keyword">if</span> o_img.getbands()[<span class="hljs-number">-1</span>] == <span class="hljs-string">'A'</span>:        print(<span class="hljs-string">"Info: 包含 alpha 通道，准备移除..."</span>)    o_img = o_img.convert(<span class="hljs-string">"RGB"</span>)    create_appicon_set(o_img,t_path=t_path,iPad=<span class="hljs-literal">True</span>)</code></pre><p>假设该程序保存为<code>appicon_generator.py</code>，选择一个目录在终端打开，然后执行命令：<code>$ python3 /path/to/appicon_generator.py /iconimg/path/1024x1024.png /path/to/output</code>，将在 <code>/path/to/output</code> 目录生成一个 <code>AppIconxxxxx.appiconset</code> （<code>xxxxx</code> 是随机数字，增加辨识度避免撞车）目录，将该目录拖动到 <code>.xcassets</code> 文件夹里面，然后在 Xcode 的 Targets -&gt; General -&gt; App Icons and Launch Images 中选择刚刚生成的 AppIcon 即可</p><h1 id="App-内部-icon-更换"><a href="#App-内部-icon-更换" class="headerlink" title="App 内部 icon 更换"></a>App 内部 icon 更换</h1><p>这里没啥好说的，一般出现在“设置”、“关于 XXX” 里面，替换即可</p><h1 id="切换"><a href="#切换" class="headerlink" title="切换"></a>切换</h1><blockquote><p>Git 的每次提交应该只做一件事！</p></blockquote><p>我们更换了 App Icons 和其他需要替换的 icon 后，立即 git 提交一次，本次提交只做一件事，就是切换 icon。等到需要将 icon 还原时，直接 <code>git revert</code> 本次提交，方便快速切换回来</p>]]></content>
    
    
    <categories>
      
      <category>开发技巧</category>
      
    </categories>
    
    
    <tags>
      
      <tag>iOS</tag>
      
      <tag>Python</tag>
      
      <tag>脚本</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
